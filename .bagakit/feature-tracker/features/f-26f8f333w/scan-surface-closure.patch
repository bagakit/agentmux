184a185,217
>       // 上面那圈只看得见**字面量直接写在构造里**的码。control-host.ts 不是这么写的：`object` / `id` /
>       // `identity` / `text` 各自把 `code: string` 当形参收下，再 `throw new AgentMuxError(message, code)`，
>       // 于是真正的码只出现在调用点（`object(v, '…', 'INVALID_CONTROL_REQUEST')`），构造里一个大写字面量
>       // 也没有。d035a948 的 message 写过「每一处调用点都是字面量」——那句话当时就是错的。
>       //
>       // 这些码今天恰好都在册（CONTROL_PROTOCOL_ERROR / INVALID_CONTROL_REQUEST），所以这不是在补一个
>       // 活着的缺陷，是在补**看不见**：只要这个形状存在，下一个经它新增的码就是静默漏网。
>       //
>       // 收的是「转发型 helper 的调用点上的字面量」，不是整个文件的大写字面量：先试过后者，当场捞回
>       // ENOENT / ECONNREFUSED / ECONNRESET / EPIPE —— 那是 errno 比较（`error.code === 'ENOENT'`），
>       // 不是 AgentMux 码。判 helper 的两个条件缺一不可：形参里有 `code`，且函数体确实把它转发给
>       // AgentMuxError。只满足前者的函数（拿 code 只为比较或透传别处）不该让它的调用点被当成 throw 点。
>       const forwarders = [...source.matchAll(/function\s+(\w+)\s*\(([^)]*)\)[^{]*\{/g)]
>         .filter((match) => /\bcode\b/.test(match[2]!))
>         .filter((match) => {
>           const body = source.slice(match.index + match[0].length)
>           const end = body.search(/\n(?:export )?(?:async )?function /)
>           return /new AgentMuxError\([^)]*,\s*code\s*\)/.test(end < 0 ? body : body.slice(0, end))
>         })
>         .map((match) => match[1]!)
>       for (const name of forwarders) {
>         // 形参默认值本身就是一个码（`text` 的 `code = 'INVALID_CONTROL_REQUEST'`），调用点不写也会抛它。
>         for (const call of source.matchAll(new RegExp(`\\b${name}\\s*\\(`, 'g'))) {
>           let depth = 0
>           let index = call.index + call[0].length - 1
>           for (; index < source.length; index += 1) {
>             if (source[index] === '(') depth += 1
>             else if (source[index] === ')') { depth -= 1; if (depth === 0) break }
>           }
>           const args = source.slice(call.index, index)
>           found.push(...[...args.matchAll(/'([A-Z][A-Z_]{3,})'/g)].map((code) => code[1]!))
>         }
>       }
212,213c245,250
<     // submission、timeline……）。给到不了的码上户口是这道门自己记过的反面教材。所以取「CLI 真正
<     // 调的那几个 client 方法所触达的模块」这一层，并由下面的断言证明这张表没有凭空写。
---
>     // submission、timeline……）。给到不了的码上户口是这道门自己记过的反面教材。
>     //
>     // 所以这一层从 **CLI 真正调的那几个 client 方法**出发，在 client.ts 内部沿 `this.X(` 求传递闭包，
>     // 再把闭包里用到的值 import 收进扫描面。上一版这里写的是一张手抄表（只有 agent-session-registry
>     // 一项），它漏掉了 handOff / startDiscussion / statusAgent 那几条线——而「手抄表会烂」正是这道门
>     // 自己反复记过的教训，当时却在它上面又抄了一张。
216,217c253,325
<     const REACHED_THROUGH_CLIENT = ['agent-session-registry']
<     for (const name of REACHED_THROUGH_CLIENT) if (!modules.includes(name)) modules.push(name)
---
>     /**
>      * 取一个类方法的源码。终点不能直接找第一条 `\n  }`：签名里的内联对象类型
>      * （`handOff(input: { … })`）自己就有一条同缩进的 `  }`，按它切会在进入函数体之前就停住，于是
>      * 这个方法的调用边一条都收不到——实测 handOff / startDiscussion 两条线就是这么丢的。所以先按
>      * 括号配平跳过参数表，从函数体的 `{` 起算。
>      */
>     const methodBody = (name: string): string => {
>       const start = clientSource.search(
>         new RegExp(`^  (?:private |public |protected )?(?:async )?${name}\\s*[(<]`, 'm')
>       )
>       if (start < 0) return ''
>       let depth = 0
>       let index = clientSource.indexOf('(', start)
>       for (; index < clientSource.length; index += 1) {
>         if (clientSource[index] === '(') depth += 1
>         else if (clientSource[index] === ')') { depth -= 1; if (depth === 0) break }
>       }
>       const open = clientSource.indexOf('{', index)
>       if (open < 0) return ''
>       const end = clientSource.indexOf('\n  }', open)
>       return clientSource.slice(open, end < 0 ? clientSource.length : end)
>     }
>     const visited = new Set<string>()
>     const queue = [...calledOnClient]
>     let reachedSource = ''
>     while (queue.length > 0) {
>       const name = queue.pop()!
>       if (visited.has(name)) continue
>       visited.add(name)
>       const body = methodBody(name)
>       reachedSource += body
>       for (const call of body.matchAll(/\bthis\.([a-zA-Z]+)\(/g)) queue.push(call[1]!)
>     }
>     // 协作者字段是第二条边，而且是最要紧的那条：`this.registry.resolve(...)` 里 `registry` 不是方法、
>     // 也不是 import 绑定名，光看名字出现与否永远收不到 agent-session-registry —— 审计找出的那个真缺陷
>     // 正在那里。所以把用到的 `this.<字段>.` 逐个回查字段声明的类型名，把类型名也算作「用到了」。
>     //
>     // 只收**这一张表点名**的字段，不是全部。全收过一次，扫描面从 10 个码涨到 56 个：kernel、
>     // providers、hookServer、promptSubmission…… 一路把 CLI 根本调不到的码全捞了进来，而给到不了的
>     // 码上户口正是这道门自己记过的反面教材。表里每一项都由一条真命令证明过够得着（见
>     // CLI_ERROR_CODES 里各自的注释），下面那条断言逐项复核它今天还是个字段。
>     const CLI_REACHED_FIELDS = ['registry', 'store']
>     const usedNames = new Set<string>()
>     for (const field of reachedSource.matchAll(/\bthis\.([a-zA-Z]+)\./g)) {
>       if (!CLI_REACHED_FIELDS.includes(field[1]!)) continue
>       const declared = clientSource.match(
>         new RegExp(`^  (?:private |public |protected )?(?:readonly )?${field[1]!}:\\s*([A-Za-z][\\w]*)`, 'm')
>       )
>       if (declared) usedNames.add(declared[1]!)
>     }
>     expect(
>       usedNames.size,
>       `CLI_REACHED_FIELDS 点名的字段一个都没解析出类型——client.ts 的字段声明写法变了，` +
>         '这层扫描此刻是空的，门恒绿'
>     ).toBe(CLI_REACHED_FIELDS.length)
>     // 闭包里出现过的值 import 就是深二层的扫描面。按 import 绑定名回查模块，只收真正被用到的那些。
>     // 这里的 import 子句**必须**允许换行（对照上面 valueImports 的单行约束）：client.ts 的
>     // `agent-session-registry` 正是写成多行大括号的，按单行匹配它整条看不见——审计找出的那个真缺陷
>     // 就在那个文件里。两处约束不同是有意的：那边扫的是 CLI（判 type-only 要贴着行首），这边扫的是
>     // client.ts（判绑定名用没用到，多行必须收）。
>     for (const match of clientSource.matchAll(
>       /^import\s+(type\s+)?([\s\S]*?)\s*from '\.\/([a-z0-9-]+)\.js'/gm
>     )) {
>       if (match[1]) continue
>       const owner = match[3]!
>       if (modules.includes(owner)) continue
>       const bindings = match[2]!.replace(/[{}]/g, '').split(',')
>         .map((binding) => binding.trim().split(/\s+as\s+/).at(-1)!.trim())
>         .filter((name) => name !== '' && !name.startsWith('type '))
>       if (bindings.some((name) => usedNames.has(name) || new RegExp(`\\b${name}\\b`).test(reachedSource))) {
>         modules.push(owner)
>       }
>     }
267,271c375,383
<     // REACHED_THROUGH_CLIENT 也要自证。两头都查，因为这张表**两个方向都会烂**：
<     //  1. client 还得是进程内建的。哪天 CLI 改成连 daemon，这些码就归控制码表管，这层扫描就该撤掉。
<     //  2. client 还真的在调它。`registry.` 一个调用点都没有了，说明这条边没了，表该瘦。
<     // 反过来「client 开始调新模块」这一侧**不设断言**：那会要求这张表跟着 client.ts 的每次改动走，
<     // 而漏掉一个模块的后果是少报（门更松），不是误报。这条边界写在这里，免得读的人以为它守得住。
---
>     // 深二层的闭包也要自证。三条前提，塌一条这层扫描就不成立：
>     //  1. client 还得是进程内建的。哪天 CLI 改成连 daemon，这些码就归控制码表管，这层该整个撤掉。
>     //  2. CLI 真的在调 client 方法（闭包的起点非空）。
>     //  3. 方法体真的切得出来——`methodBody` 靠的是「两空格缩进的签名行 + 第一条 `\n  }`」这个约定，
>     //     client.ts 换了格式化风格就会静默切出空串，闭包随之塌成空集，而空集上的扫描是**白绿**。
>     //
>     // 上一版这里守的是一张手抄的模块表，还写明「client 开始调新模块」那一侧故意不设断言。那个决定
>     // 是错的：它把「少报＝门更松」当成可接受，而门松掉的恰恰就是这道门唯一的用途。现在扫描面由
>     // `calledOnClient` 沿 `this.X(` 求闭包导出，不再有手抄表，那条边界也就不存在了。
274c386
<       'CLI 不再进程内建 client 了——REACHED_THROUGH_CLIENT 这层扫描的前提没了'
---
>       'CLI 不再进程内建 client 了——这层深二层扫描的前提没了'
276,282c388,393
<     for (const name of REACHED_THROUGH_CLIENT) {
<       expect(
<         clientSource.includes(`this.registry.`),
<         `client.ts 不再调 ${name} 了——这条深二层的边没了，把它从 REACHED_THROUGH_CLIENT 里删掉`
<       ).toBe(true)
<     }
<     expect(calledOnClient.size, 'CLI 一个 client 方法都没调——深二层扫描的前提不成立').toBeGreaterThan(3)
---
>     expect(calledOnClient.size, 'CLI 一个 client 方法都没调——深二层扫描的前提不成立').toBeGreaterThan(3)
>     expect(
>       visited.size,
>       `闭包只展开到 ${visited.size} 个方法，比 CLI 直接调的 ${calledOnClient.size} 个还少——` +
>         'methodBody 多半没切出方法体（client.ts 的缩进/格式变了），此时扫描面是空的，门恒绿'
>     ).toBeGreaterThan(calledOnClient.size)
292a404,409
>     // 第二个真缺陷，来自对上一版的审计：handOff / startDiscussion 经能力校验到 agent-capability.ts，
>     // 那几个码同样折成 AGENTMUX_FAILED。手抄表看不见它们，闭包能。
>     expect(
>       [...reachable.keys()],
>       '扫描面漏掉了 agent-capability.ts——handOff/startDiscussion 这条线上的码会静默折掉'
>     ).toContain('AGENT_CAPABILITY_INVALID')
