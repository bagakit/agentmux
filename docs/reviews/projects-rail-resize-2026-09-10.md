# Projects 栏拖拽调宽评审（2026-09-10）

## 结论

用户明确要求最左侧 Projects 菜单支持拖拽改变宽度。该需求与 Feature `f-24v8fec2k` 现有本地文件预览 Closure 不同，但按用户指定归入该 Feature，作为后续任务记录；本次不宣称已实现。

## 验收边界

Projects 栏与主工作区之间提供可发现的拖拽分隔带；拖拽时宽度和内容即时适配，松开后宽度可恢复；不改变 Workspace Tools 或主工作区的布局身份；键盘和辅助技术可访问。实现应复用现有宽度 owner/persistence 模式，不新增第二份布局状态。
