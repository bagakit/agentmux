# 启动配置修复与诊断

Review: approved — 用户明确要求检查启动退出和日志，并确认直接适配已安装配置。实施范围是修复这次配置、保留既有会话引用、补齐启动失败持久留证并验证安装启动。无需配置迁移或兼容层。

证据：当前 schema 不接受 executors.*.identity；当前日志捕获未捕获异常，但已捕获的启动失败仅写 stderr，Finder 启动后无法回看。现有 16 个 Session 引用旧执行器 ID，不单独改键。

验证：配置修复前后校验无 Session 引用丢失；启动错误行为测试、持久写入测试、变异红测及生产调用点；打包与真实安装启动。
