# Default Session 浮窗交付补齐

用户明确要求浮窗照抄 a mature workbench 并彻底实现。本 review 状态 approved，延续原用户目标；没有新增产品线。

前一 Feature f-29s8fwf9m 的 gate 只检查几何函数和源码字符串，却把关闭保活、焦点返还和重启恢复判为完成。这些结论无行为证据，归档过早。后续以真实组件、Store 和持久恢复验证修正，不能把已有绿灯冒充交付。

范围：唯一默认 Topic 的原生工作台保活；不改变底下主面与非默认 Topic；窗口拖动、缩放、最大化/还原、收起和焦点返还；生产快捷键和新建动作落在浮窗内；统一持久状态、可见性与内存回收；重启可恢复。继续保留 Core/ctxmux 唯一事实所有权。最终合入 main，保留并行改动。
