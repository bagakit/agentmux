# 项目导航：目录分组与作为项目打开

Review: approved

依据：用户 2026-09-09 明确要求“视觉结构可以优雅又清晰”“目录右键操作里加上作为项目打开”“写进文档和 tracker 并推进”。本计划落实已授权范围，不新增 Runtime 或磁盘动作。

Closure：左侧分组归属可辨，文件树目录菜单可以注册并激活项目；已有同 host/path 的项目直接激活。复用 typed workspace API 和既有错误展示。

任务边界：菜单行为和分组结构可各自验证，无互相依赖；最终打包检查消费两者完成后的候选。保留用户已有未提交文件；打包不安装、不重启当前 App。

验证：行为测试、变异红绿、排除定义文件的生产调用检查；类型检查、构建和 macOS 打包。包内 Runtime 与完整产品可用性以实际 gate 结果报告，不以构建通过替代。
