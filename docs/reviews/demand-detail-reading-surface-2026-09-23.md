# Demand detail reading surface review

Status: approved from the user's screenshot and request on 2026-09-23.

The current right pane duplicates the title, exposes every field as a bordered form control, and spends much of the first screen on empty assignment and topology states. The accepted outcome is a readable Demand workspace: title and description first, key routing properties visible, secondary metadata disclosed on demand, and Session execution shown only when it has facts to show. Existing Demand editing and Session observation remain available.

Acceptance is a mounted Board interaction with a persisted Demand, including zero and multiple Session states, narrow layout, keyboard-accessible editing, and a regression check that title/description edits still update the same Demand.
