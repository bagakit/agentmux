# 新 Session、Redraw 与控件复查

用户于本轮明确要求修复 Connecting、无效 Redraw／缩放恢复后提示不消失、细描边无发光、左上可编辑角标、中性平面打断按钮。状态：approved（来自用户本轮指令）；当前已装00904221。

连接与画面属于同一可用终端闭环，视觉精修属于既有消息控件Closure。三条实现可并发，集成必须保留活Run、Session身份及布局，验证真实新建和重启恢复。已发现双次Renderer加载和单attach全量status扇出，但新建Connecting须以现场及测试定位，不能仅按相似症状推断。优先修事实拥有者，不用重试层掩盖。每条有行为验证、有效变异和定义文件外真实消费者。

2026-09-21 用户追加加载页：更强动势、后现代／解构风格，显示目标 Executor 与初始 Prompt，Prompt 旁可复制。状态 approved，来源为用户明确指令。同一连接可用性 Closure 的可恢复信息表面，添加独立 T-003 并行实现。轻量 design-core review：工具属性优先，错位／切片只用于装饰和标题；信息读取及复制不移动、不闪烁；真实320/420px界面与reduced-motion验证。T-001 verification 中单复数测试文件笔误同次修正，不改变验收。

2026-09-21 用户明确 Executor 标识属于 Execution 配置，具体 Agent 的所有图标统一显示 Executor 标识。approved；同一消息／身份控件 Closure 新增 T-006，替代先前入口归于 Appearance 的选择。默认 Provider 底图、左上定制、右上状态互相独立，不增加第二份配置。
