# Browser RSI 验收修复

Review: approved

授权来源：用户要求完成 Browser 自主观察、行动、复盘、回放与人机交接，并持续要求“彻底完成”“继续”。本计划只修复原 `f-2778fzy4c` 的实现与验收缺口；主代理明确委派了本次复核和四项修复任务。原归档的事实与审计更正见 [原 review](browser-rsi-feature-2026-09-19.md)，不重写历史状态。

Closure：同一个 Main-owned operation 在 CLI、Browser 操作条、历史 timeline、页面提示与安全回放中可观察；人可明确停止并交还，敏感步骤保留闸门，journal 故障可见且不阻断健康页面；真实 Electron 与交互测试对最终候选证明这条链路。

分工与依赖：T-001 只拥有 journal 及其测试；T-002 由主代理统一共享 Browser 合同、manager、IPC 和 Renderer/UI 的接线；T-003 拥有 Core CLI、帮助与相关测试，消费 T-002 的 API。三者均可先验证自身独立结果，由 T-004 汇合实际生产接线与真机证据。共同文件的变更由主代理顺序整合，不使用 stash、reset、分支切换或覆盖并行修改。

验收纠正：保留需要人确认的步骤、失败/未知结局和裁剪缺口；不能以删行、空集合或只测纯函数满足通过。每项任务提供行为测试、变异红绿和排除定义文件后的生产调用者证据。最终 Browser 回归显式包含 `.test.ts` 和 `.test.tsx`；类型检查和构建针对最终工作树，归档以实际测试与可达 UI 为准。最终候选记录 HEAD、工作树摘要和测试输入身份；不为取得干净状态而清理其他人的工作。

范围不变：不做站点或厂商专用分支，不记录原始密码/Cookie/敏感输入，不自动夺回人已接管的页面，不添加另一套 Runtime 或浏览器动作模型。
